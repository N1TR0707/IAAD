# Konfigurasi produksi frontend.
# Karena frontend & API satu domain (via Nginx proxy), pakai path relatif.
# Saat build di server: salin file ini jadi .env.production lalu jalankan `npm run build`.
VITE_API_URL=/api
